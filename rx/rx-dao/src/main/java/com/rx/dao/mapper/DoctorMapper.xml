<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rx.dao.DoctorMapper">
  <resultMap id="BaseResultMap" type="com.rx.entity.Doctor">
    <!--
      WARNING - @mbggenerated
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="old_id" jdbcType="VARCHAR" property="oldId" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="department_id" jdbcType="BIGINT" property="departmentId" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbggenerated
    -->
    id, old_id, name, department_id
  </sql>
  
  <select id="getDoctorByPatientId" resultType="map">	
	SELECT d.* from doctor AS d LEFT JOIN doctor_patient AS dp ON(d.id=dp.doctor_id) 
	WHERE dp.patient_id=#{patient_id};
  </select>
  
  
</mapper>