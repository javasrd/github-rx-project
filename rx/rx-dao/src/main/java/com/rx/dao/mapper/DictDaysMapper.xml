<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.rx.dao.DictDaysMapper" >
  <resultMap id="BaseResultMap" type="com.rx.entity.DictDays" >
    <!--
      WARNING - @mbggenerated
    -->
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="code" property="code" jdbcType="VARCHAR" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="value" property="value" jdbcType="INTEGER" />
  </resultMap>
  <sql id="Base_Column_List" >
    <!--
      WARNING - @mbggenerated
    -->
    id, code, name, value
  </sql>
  
  <select id="getDaysByAbc" resultMap="BaseResultMap">  	
		SELECT 
			* 
		FROM 
			dict_days as m		 
		WHERE			
			m.code LIKE CONCAT('%',#{abc},'%')
		
  </select>
  
  <select id="getDaysByDaysName" resultType="map">  	
		SELECT 
			* 
		FROM 
			dict_days as m		 
		WHERE			
			m.name LIKE CONCAT('%',#{name},'%')
		
  </select>
  
  
  
</mapper>