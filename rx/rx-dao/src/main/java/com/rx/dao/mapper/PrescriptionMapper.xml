<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rx.dao.PrescriptionMapper">
  <resultMap id="BaseResultMap" type="com.rx.entity.Prescription">
    <!--
      WARNING - @mbggenerated
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="doctor_id" jdbcType="BIGINT" property="doctorId" />
    <result column="patient_id" jdbcType="BIGINT" property="patientId" />
    <result column="created_time" jdbcType="TIMESTAMP" property="createdTime" />
    <result column="rx_no" jdbcType="VARCHAR" property="rxNo" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbggenerated
    -->
    id, doctor_id, patient_id, created_time, rx_no
  </sql>
  
  <select id="getMaxPrescriptionNo" resultType="string">
		SELECT max(rx_no) rxNo from prescription WHERE rx_no like
		CONCAT('%',#{dateCond},'%' )
  </select>
  
  <!-- 根据条件查询处方信息 -->
  <select id="getPrescInfoByCondition" resultType="map" parameterType="map">
		SELECT
			pr.id AS presc_id,
			pr.doctor_id,
			pa.id AS patient_id,
			pr.created_time,
			pr.rx_no,
			pa.old_id,
			pa.`name`,
			pa.sex,
			pa.old,
			pa.rn
		FROM
			prescription pr
		LEFT JOIN patient pa ON pr.patient_id = pa.id
			
		<where>
			doctor_id = #{doctorId}
			<if test="keyword!=null and keyword!=''">
	            and (pr.rx_no LIKE CONCAT("%",#{keyword},"%") or pa.`name` LIKE CONCAT("%",#{keyword},"%"))
	        </if>
	        <if test="startTime!=null and startTime!=''">
	            and pr.created_time &gt;= #{startTime}
	        </if>
	        <if test="endTime!=null and endTime!=''">
	            and pr.created_time &lt; #{endTime}
	        </if>
		</where>	
  </select>
  
</mapper>